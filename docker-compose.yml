services:
  responsiveviewer:
    image: responsiveviewer
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
      REACT_APP_PLATFORM: LOCAL
    ports:
      - 3000:3000
